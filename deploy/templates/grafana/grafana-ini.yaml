{{- if .Values.grafana.enabled }}
{{- $proto := .Values.acs.secure | ternary "https://" "http://" }}
{{- $openid := printf "%sopenid.%s" $proto (.Values.acs.baseUrl | required "values.acs.baseUrl is required") }}
{{- $grafana := printf "%sgrafana.%s" $proto (.Values.acs.baseUrl | required "values.acs.baseUrl is required") }}
{{- $realm := printf "%s/realms/%s" $openid (.Values.openid.realm | required "values.openid.realm is required") }}
{{- $perms := .Values.serviceSetup.config.grafanaPermissions | required "values.serviceSetup.config.grafanaPermissions is required" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: acs-grafana-config
  namespace: {{ .Release.Namespace }}
data:
  root_url: {{ $grafana | quote }}
  auth_url: {{ printf "%s/protocol/openid-connect/auth" $realm | quote }}
  token_url: {{ printf "%s/protocol/openid-connect/token" $realm | quote }}
  api_url: {{ printf "%s/protocol/openid-connect/userinfo" $realm | quote }}
  signout_redirect_url: {{ printf "%s/protocol/openid-connect/logout?post_logout_redirect_uri=%s&client_id=grafana" $realm $grafana | quote }}
  # JMESPath that translates the SPI's fp_permissions JWT claim into
  # the matching Grafana role. Non-matching users fall through to
  # Viewer. UUIDs come from serviceSetup.config.grafanaPermissions; the
  # same UUIDs are seeded by service-setup as F+ Permission objects, so
  # admins grant them via the standard ACL editor.
  #
  # not_null(fp_permissions, `[]`) is load-bearing: Keycloak's
  # OIDCAttributeMapperHelper omits the claim entirely (rather than
  # emitting an empty array) when the user has zero F+ permission
  # grants. Without the not_null guard, contains() gets null as its
  # subject and go-jmespath (what Grafana uses) leaks a single space
  # through the || chain instead of falling through to 'Viewer'. The
  # net effect with role_attribute_strict=true is "Integration
  # requires a valid org role assigned in idP. Assigned role: ' '"
  # and login is refused for any user without a Grafana grant.
  role_attribute_path: >-
    contains(not_null(fp_permissions, `[]`)[*], '{{ $perms.admin }}') && 'Admin'
    || contains(not_null(fp_permissions, `[]`)[*], '{{ $perms.editor }}') && 'Editor'
    || 'Viewer'
{{- end }}
