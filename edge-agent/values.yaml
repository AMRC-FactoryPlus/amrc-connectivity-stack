image:
  # Parameters for the Edge Agent image to pull
  edgeAgent:
    #registry: ghcr.io/amrc-factoryplus
    repository: acs-edge
    #tag: "v2.0.0"
    #pullPolicy: IfNotPresent
    registry: amrc-factoryplus.shef.ac.uk:5000
    tag: "v2.0.0-bmz"
    pullPolicy: Always
debug: true
poll_int: 10
authGroup:
  # This must be supplied on deployment. It specifies the Auth group to
  # place the Edge Agent user account in.
  edgeAgent: fc64ced3-c186-405c-81ee-8d11a261ce8f
limits:
  cpu: "100m"
  memory: "200Mi"
