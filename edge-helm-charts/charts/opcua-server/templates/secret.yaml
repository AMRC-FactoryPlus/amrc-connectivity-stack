{{- $k8sname := include "opcua-server.k8sname" .Values.name }}
{{- $userSecret := printf "opcua-server-opcua-username.%s" $k8sname }}
{{- $passSecret := printf "opcua-server-opcua-password.%s" $k8sname }}
apiVersion: v1
kind: Secret
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ $userSecret }}
  labels:
    factory-plus.app: opcua-server
    factory-plus.uuid: {{ .Values.uuid }}
type: Opaque
stringData:
  username: {{ .Values.opcua.username | quote }}
---
apiVersion: factoryplus.app.amrc.co.uk/v1
kind: LocalSecret
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ $passSecret }}.password
  labels:
    factory-plus.app: opcua-server
    factory-plus.uuid: {{ .Values.uuid }}
spec:
  format: Password
  secret: {{ $passSecret }}
  key: password
