$kind: http-request
url: '{{scheme}}://auth.{{domain}}/authz/principal/find'
method: GET
queryParams:
  kerberos: user@REALM
order: 2000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200]);'
    - '});'
