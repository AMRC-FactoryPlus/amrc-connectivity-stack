$kind: http-request
url: '{{scheme}}://auth.{{domain}}/v2/principal/:uuid'
method: GET
order: 4000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200 / 404', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200, 404]);'
    - '});'
