$kind: http-request
url: "{{scheme}}://clusters.{{domain}}/v1/cluster/:cluster/secret/:namespace/:name/:key"
method: PUT
queryParams:
  dryrun: "true"
body:
  type: text
  content: <plaintext secret>
order: 5000
