$kind: http-request
description: Direct members only.
url: '{{scheme}}://configdb.{{domain}}/v2/class/:class/direct/member'
method: GET
order: 9000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200 / 404', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200, 404]);'
    - '});'
