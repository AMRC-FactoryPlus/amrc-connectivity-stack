$kind: http-request
description: All effective members (transitive via subclass).
url: '{{scheme}}://configdb.{{domain}}/v2/class/:class/member'
method: GET
order: 8000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200 / 404', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200, 404]);'
    - '});'
