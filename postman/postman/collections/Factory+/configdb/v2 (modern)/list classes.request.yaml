$kind: http-request
description: Members of Class.Class.
url: '{{scheme}}://configdb.{{domain}}/v2/class'
method: GET
order: 6000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200]);'
    - '});'
