$kind: http-request
url: '{{scheme}}://directory.{{domain}}/v1/alert/type/:type/active'
method: GET
order: 4000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200 / 404', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200, 404]);'
    - '});'
