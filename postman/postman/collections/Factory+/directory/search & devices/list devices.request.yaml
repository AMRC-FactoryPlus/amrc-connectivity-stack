$kind: http-request
url: '{{scheme}}://directory.{{domain}}/v1/device'
method: GET
order: 2000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200]);'
    - '});'
