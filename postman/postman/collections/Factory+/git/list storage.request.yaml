$kind: http-request
url: '{{scheme}}://git.{{domain}}/v1/storage'
method: GET
order: 3000
events:
- listen: test
  script:
    type: text/javascript
    exec:
    - pm.test('Status is 200', function () {
    - '    pm.expect(pm.response.code).to.be.oneOf([200]);'
    - '});'
