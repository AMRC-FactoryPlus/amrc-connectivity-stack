apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: manager-ingressroute
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`manager.{{.Values.baseUrl | required "values.baseUrl is required"}}`)
      kind: Rule
      services:
        - name: manager
          port: 80
          namespace: {{ .Values.namespace | required "values.namespace is required!" }}
  tls:
    secretName: {{ .Values.tlsSecretName | required "values.tlsSecretName is required!" }}
    domains:
      - main: manager.{{.Values.baseUrl | required "values.baseUrl is required"}}